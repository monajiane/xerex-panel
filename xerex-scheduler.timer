# ============================================================================
# Xerex Panel — scheduler timer
# Pair with xerex-scheduler.service.
# ============================================================================

[Unit]
Description=Run Xerex Panel scheduler every minute

[Timer]
OnCalendar=*:*:00
Persistent=true
AccuracySec=5s
Unit=xerex-scheduler.service

[Install]
WantedBy=timers.target
